/* Name: Kevin Kan
 * Date: June 14 2015
 * Purpose: Create a program that accepts two fractions as strings and does, addition, subraction, multiplicaiton and division; 
 * then display the results in a formatted table
 */
#include <iomanip>
#include <iostream>
#include <cmath>
#include <string>

using namespace std;
/*addFrac adds two fractions together and returns the results
*
*/
char* addFrac(char *frac1, char *frac2)
{
	char *sum;
	return sum;
}

/*subtractFrac subtracts two fractions
*
*/
char* subtractFrac(char *frac1, char *frac2)
{
	char *sum;
	return sum;
}

/*multiplyFrac multiplies two fractions
*/
char* multiplyFrac(char *frac1, char *frac2)
{
	char *product;
	return product;
}

/* divFrac divides two fractions
*/
char* divFrac(char *frac1, char *frac2)
{
	char *quotient;
	return quotient;
}

/* reduceFrac reduces the fraction into lowest value
*/
string reduceFrac(char *frac1)
{
	atoi("66");
	int numerator, denominator;
	string full;

	return full;
}

//main funciton to run
int main()
{

}